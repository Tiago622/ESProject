<%= form_with(model: lesson_schedule, local: true) do |form| %>
  <% if lesson_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_schedule.errors.count, "error") %> prohibited this lesson_schedule from being saved:</h2>

      <ul>
      <% lesson_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :lesson_name, class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= collection_select(:lesson_schedule, :lesson_id, Lesson.all, :id, :name, {}, {class: "custom-select"}) %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :schedule_name, class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= collection_select(:lesson_schedule, :schedule_id, Schedule.all, :id, :name, {}, {class: "custom-select"}) %>
    </div>
  </div>

  <div class="actions">
    <%= link_to 'Back', lesson_schedules_path, class: "btn btn-secondary" %>
    <%= form.submit class: "btn btn-dark" %>
  </div>
<% end %>
