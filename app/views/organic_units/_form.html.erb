<%= form_with(model: organic_unit, local: true) do |form| %>
  <% if organic_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organic_unit.errors.count, "error") %> prohibited this organic_unit from being saved:</h2>

      <ul>
      <% organic_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :acronym %>
    <%= form.text_field :acronym %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
